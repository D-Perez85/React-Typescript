import Navigation from "../01-lazyload/router/Navigation";

export const LazyLayout = () => {
  return (
    <div>
      <h1>Lazy Layout Page - MAIN</h1>
      <Navigation/>
    </div>
  );
};

export default LazyLayout;
